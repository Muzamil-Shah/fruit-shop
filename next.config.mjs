/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
                hostname: "woozy-falcon-702.convex.cloud"
            },
        ],
      },
    };
    
    export default nextConfig;
 